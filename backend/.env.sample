# ==========================================
# Backend Environment Configuration
# ==========================================

# App Mode Configuration
# Options: development, production, demo
# - development: Full auth enabled, development features
# - production: Full auth enabled, optimized for production
# - demo: Authentication disabled, uses demo user
APP_MODE=development

# Port Configuration
# Backend server port
BACKEND_PORT=8000

# Frontend server port (used for CORS configuration)
FRONTEND_PORT=5173

# ==========================================
# Microsoft Entra ID Authentication
# ==========================================

# Azure/Entra ID Tenant ID
# Get this from Azure Portal > Entra ID > Overview > Tenant ID
AZURE_TENANT_ID=your-azure-tenant-id-here

# Azure/Entra ID Application (Client) ID for client apps (React/Flutter)
# Get this from Azure Portal > Entra ID > App registrations > Your Client App > Overview
AZURE_CLIENT_ID=your-azure-client-id-here

# Azure/Entra ID Application (Client) ID for API app
# Get this from Azure Portal > Entra ID > App registrations > Your API App > Overview
AZURE_API_CLIENT_ID=your-azure-api-client-id-here

# ==========================================
# FastAPI Documentation Settings
# ==========================================

# FastAPI Swagger UI documentation endpoint
# Set to "null" to disable in production
DOCS_URL=/docs

# FastAPI ReDoc documentation endpoint
# Set to "null" to disable in production
REDOC_URL=/redoc

# OpenAPI JSON schema endpoint
# Set to "null" to disable in production
OPENAPI_URL=/openapi.json

# ==========================================
# PostgreSQL Database Configuration
# ==========================================

# Database connection settings
DB_HOST=your-database-host-here
DB_PORT=5432
DB_NAME=your-database-name-here
DB_USER=your-database-user-here
DB_PASSWORD=your-database-password-here

# SSL mode for database connection (prefer, require, disable)
# Use "prefer" for development, "require" for production
DB_SSL_MODE=disable

# ==========================================
# Azure OpenAI Configuration
# ==========================================

# Azure OpenAI service endpoint
# Format: https://your-resource-name.openai.azure.com/
AZURE_OPENAI_ENDPOINT=https://your-openai-endpoint-here.openai.azure.com/

# Azure OpenAI API key
AZURE_OPENAI_API_KEY=your-azure-openai-api-key-here

# Azure OpenAI API version
AZURE_OPENAI_API_VERSION=2025-02-01-preview

# Azure OpenAI deployment names
AZURE_OPENAI_CHAT_DEPLOYMENT=gpt-4o
AZURE_OPENAI_EMBEDDING_DEPLOYMENT=text-embedding-3-small

# ==========================================
# Semantic Kernel Configuration
# ==========================================

# Enable OpenTelemetry diagnostics for sensitive data in Semantic Kernel
# Set to "true" to enable detailed telemetry including prompts and responses
# Set to "false" to disable sensitive data collection (recommended for production)
SEMANTICKERNEL_EXPERIMENTAL_GENAI_ENABLE_OTEL_DIAGNOSTICS_SENSITIVE=false
